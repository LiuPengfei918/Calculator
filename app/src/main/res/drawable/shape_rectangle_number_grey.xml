<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!--点击数字按钮后变色效果的颜色-->
    <solid android:color="@color/colorNumber_grey"/>
    <!-- 边框设置-->
    <stroke
        android:width="1dp"
        android:color="@color/colorMain"/>

</shape>