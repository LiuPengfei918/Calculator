<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!--数字按钮颜色设置-->
    <solid android:color="@color/colorNumber"/>
    <!-- 边框设置-->
    <stroke
        android:width="1dp"
        android:color="@color/colorMain"/>

</shape>